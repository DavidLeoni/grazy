import * as defs from "./defs";
import * as refImpl from "./ref-impl";


let c = defs;
// c = <any> refImpl;  todo for now we keep using defs as implementation 
// for complete working trick see
//  test/src/test-modules-3, 3-impl, 4 and gralang-test.ts

 
export default defs;
